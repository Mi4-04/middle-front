query GetTracksForGuest($query: GetTracksByPlaylistInput!) {
  getTracksForGuest(query: $query) {
    tracks {
      id
      realId
      name
      artist
      imageUrl
      audioUrl
      available
    }
    count
  }
}
